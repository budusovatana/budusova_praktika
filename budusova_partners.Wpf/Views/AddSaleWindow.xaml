<Window x:Class="budusova_partners.Wpf.Views.AddSaleWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:budusova_partners.Wpf.Views"
        mc:Ignorable="d"
         Title="Добавление продажи"
        Height="350"
        Width="450"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="140"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0"
                   Grid.Column="0"
                   Margin="0,0,10,10"
                   VerticalAlignment="Center"
                   Text="Партнер:"/>

        <TextBox x:Name="PartnerNameTextBox"
                 Grid.Row="0"
                 Grid.Column="1"
                 Margin="0,0,0,10"
                 IsReadOnly="True"
                 Background="AliceBlue"/>

        <TextBlock Grid.Row="1"
                   Grid.Column="0"
                   Margin="0,0,10,10"
                   VerticalAlignment="Center"
                   Text="Продукт:"/>

        <ComboBox x:Name="ProductsComboBox"
                  Grid.Row="1"
                  Grid.Column="1"
                  Margin="0,0,0,10"
                  DisplayMemberPath="Name"
                  SelectedValuePath="Id"
                  Background="AliceBlue"/>

        <TextBlock Grid.Row="2"
                   Grid.Column="0"
                   Margin="0,0,10,10"
                   VerticalAlignment="Center"
                   Text="Количество:"/>

        <TextBox x:Name="QuantityTextBox"
                 Grid.Row="2"
                 Grid.Column="1"
                 Margin="0,0,0,10"
                 Background="AliceBlue"/>

        <TextBlock Grid.Row="3"
                   Grid.Column="0"
                   Margin="0,0,10,10"
                   VerticalAlignment="Center"
                   Text="Цена за единицу:"/>

        <TextBox x:Name="UnitPriceTextBox"
                 Grid.Row="3"
                 Grid.Column="1"
                 Margin="0,0,0,10"
                 Background="AliceBlue"/>

        <TextBlock Grid.Row="4"
                   Grid.Column="0"
                   Margin="0,0,10,10"
                   VerticalAlignment="Top"
                   Text="Дата продажи:"/>

        <DatePicker x:Name="SaleDatePicker"
                    Grid.Row="4"
                    Grid.Column="1"
                    Margin="0,0,0,10"
                    Background="AliceBlue"/>

        <StackPanel Grid.Row="5"
                    Grid.ColumnSpan="2"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right">
            <Button Content="Сохранить"
                    Width="100"
                    Margin="0,10,10,0"
                    Click="SaveButton_Click"/>
            <Button Content="Отмена"
                    Width="100"
                    Margin="0,10,0,0"
                    Click="CancelButton_Click"/>
        </StackPanel>
    </Grid>
</Window>